// variables2.rs
// Make me compile! Execute the command `rustlings hint variables2` if you want a hint :)



fn main() {
    let x = 10; // This will evaluate to true, so the first message will be printed
    if x == 10 {
        println!("Ten!");
    } else {
        println!("Not ten!");
    }
}
